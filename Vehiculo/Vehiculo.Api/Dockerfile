FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
WORKDIR /src
COPY ["Vehiculo.Api.csproj", "./"]
RUN dotnet restore "Vehiculo.Api.csproj"
COPY . .
RUN dotnet build "Vehiculo.Api.csproj" -c Release -o /app/build

FROM build AS publish
RUN dotnet publish "Vehiculo.Api.csproj" -c Release -o /app/publish /p:UseAppHost=false

FROM mcr.microsoft.com/dotnet/aspnet:10.0 AS final
WORKDIR /app
COPY --from=publish /app/publish .
ENTRYPOINT ["dotnet", "Vehiculo.Api.dll"]
